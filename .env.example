# Application Port - Change if 80 is already in use
PORT=80

# API URL that will be used by both nginx and the frontend
# This should point to where your API is running
# Example: If your API is running in another docker-compose on port 3001
API_URL=http://localhost:3001